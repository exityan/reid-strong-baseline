from setuptools import find_packages, setup

setup(
    name="reid-strong-baseline",
    version="0.1.0",
    description="Bag of Tricks and A Strong Baseline for Deep Person Re-identification",
    packages=find_packages(),
)
